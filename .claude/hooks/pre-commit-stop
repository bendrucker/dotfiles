#!/usr/bin/env bash
set -euo pipefail

hook_dir="$(cd "$(dirname "$0")" && pwd)"
source "$hook_dir/pre-commit"

input=$(cat)
transcript_path=$(echo "$input" | jq -r '.transcript_path // empty')

if [[ -z "$transcript_path" || ! -f "$transcript_path" ]]; then
  exit 0
fi

# Collect file_path inputs from Edit/Write/MultiEdit tool calls, keeping only
# files that still exist (deleted or renamed files have nothing left to check).
# Parse line by line so one malformed transcript line can't break the rest.
existing_files=()
while IFS= read -r line; do
  [[ -z "$line" ]] && continue
  paths=$(jq -r '
    select(.type == "assistant") | .message.content[]?
    | select(.type == "tool_use" and (.name == "Edit" or .name == "Write" or .name == "MultiEdit"))
    | .input.file_path // empty
  ' <<< "$line" 2>/dev/null) || continue
  while IFS= read -r f; do
    [[ -n "$f" && -f "$f" ]] && existing_files+=("$f")
  done <<< "$paths"
done < "$transcript_path"

if [[ ${#existing_files[@]} -gt 0 ]]; then
  deduped=()
  while IFS= read -r f; do
    deduped+=("$f")
  done < <(printf '%s\n' "${existing_files[@]}" | sort -u)
  existing_files=("${deduped[@]}")
fi

if [[ ${#existing_files[@]} -eq 0 ]]; then
  exit 0
fi

# First pass: let fixers (trailing-whitespace, end-of-file-fixer, etc.) auto-fix.
$runner run --files "${existing_files[@]}" > /dev/null 2>&1 || true

# Second pass: check for remaining issues after auto-fix.
output=$($runner run --files "${existing_files[@]}" 2>&1) && exit 0

jq -n --arg reason "$runner found issues that could not be auto-fixed:

$output" \
  '{decision: "block", reason: $reason}'
