#!/usr/bin/env bash
set -euo pipefail

export PREK_HOME="${PREK_HOME:-${TMPDIR:-/tmp}/prek}"

output=$(prek run --all-files 2>&1) && exit 0

jq -n --arg reason "prek found issues. Fix them before finishing:

$output" \
  '{decision: "block", reason: $reason}'
