# Catppuccin flavors, auto-switched by yazi between dark and light based on the
# terminal appearance. The flavor packages supply the full palette and are
# installed via `ya pkg install` (pinned in package.toml) into
# ~/.config/yazi/flavors/.
[flavor]
dark = "catppuccin-mocha"
light = "catppuccin-latte"
