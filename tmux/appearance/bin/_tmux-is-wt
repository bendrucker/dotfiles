#!/usr/bin/env bash
set -euo pipefail
dir=$(git -C "$1" rev-parse --path-format=absolute --git-dir 2>/dev/null) || exit 0
common=$(git -C "$1" rev-parse --path-format=absolute --git-common-dir 2>/dev/null) || exit 0
[[ "$dir" != "$common" ]] && printf ' '
